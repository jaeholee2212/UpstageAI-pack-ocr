version: "3.3"

networks:
  infra-private:
    external: true

services:
  swarmpit:
    image: swarmpit/swarmpit:latest
    environment:
      - SWARMPIT_DB=http://tasks.couchdb:5984
      - SWARMPIT_INFLUXDB=http://tasks.influxdb:8086
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - infra-private
    logging:
      driver: "gelf"
      options:
        gelf-address: ${INFRA_GELF_ADDRESS}
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 1024M
        reservations:
          cpus: "0.25"
          memory: 512M
      placement:
        constraints:
          - node.role == manager
      labels:
        - traefik.enable=true
        - traefik.docker.network=infra-private
        - traefik.http.routers.swarmpit.rule=Host(`admin.${INFRA_DOMAIN}`)
        - traefik.http.routers.swarmpit.entrypoints=https
        - traefik.http.services.swarmpit.loadbalancer.server.port=8080
        - traefik.public.service=yes
  agent:
    image: swarmpit/agent:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - infra-private
    logging:
      driver: "gelf"
      options:
        gelf-address: ${INFRA_GELF_ADDRESS}
    deploy:
      mode: global
      resources:
        limits:
          cpus: "0.10"
          memory: 64M
        reservations:
          cpus: "0.05"
          memory: 32M
